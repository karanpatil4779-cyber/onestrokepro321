{
  "name": "onestrokepro",
  "private": true,
  "scripts": {
    "server": "npm --prefix server run start",
    "client": "npm --prefix client run dev -- --host 0.0.0.0",
    "dev": "concurrently \"npm --prefix server run start\" \"npm --prefix client run dev -- --host 0.0.0.0\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
